import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {main: '#31323c'},
        secondary: {main: '#393945'},
        background: {default: '#31323c'}
    },
})

export default theme;